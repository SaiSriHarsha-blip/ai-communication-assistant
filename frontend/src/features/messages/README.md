# Messages Feature

Why it exists: the message generator is the product's first feature boundary.

What it does: Phase 2 should add the form, selectors, generated message cards, clarification state, and copy actions here.

How it interacts: feature components consume hooks and UI primitives, while the app route composes the feature.
