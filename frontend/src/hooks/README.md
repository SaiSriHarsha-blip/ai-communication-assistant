# Hooks

Why it exists: hooks isolate client state and side effects from presentational components.

What it does: Phase 2 should add message generation and clipboard hooks here.

How it interacts: feature components import hooks to submit forms, manage loading, and show results.
